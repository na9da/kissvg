{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build -- --censor-lib --strict",
    "build-chrome": "pulp browserify -j 4 -O -m PageScript --to chrome/dist/PageScript.js",
    "test": "pulp test"
  },
  "devDependencies": {
    "pulp": "^12.0.0",
    "purescript": "^0.12.0",
    "purescript-psa": "^0.5.0",
    "rimraf": "^2.5.0"
  },
  "dependencies": {
    "bower": "^1.8.4",
    "http-server": "^0.11.1"
  }
}
